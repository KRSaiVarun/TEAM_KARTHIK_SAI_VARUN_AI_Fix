## Packages
framer-motion | Smooth animations for status updates and list entries
date-fns | Date formatting for timestamps

## Notes
- Tailwind config needs to include 'JetBrains Mono' for code elements.
- The backend API uses standard REST conventions defined in shared/routes.ts.
- Status polling will be needed for the project details page to update from 'running' to 'completed'.
